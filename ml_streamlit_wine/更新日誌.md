# 📜 ML-Streamlit 專案更新日誌 (Changelog)

本文件紀錄了今天從零開始建立「紅酒分類機器學習專題」的所有關鍵步驟與開發里程碑。

---

## 🕒 2026-03-03 開發紀錄

### 1. 專案啟動與環境建置
*   **目標**：建立 Python 3.12 環境，並安裝開發機器學習與網頁應用所需的套件。
*   **關鍵操作**：
    *   建立專案目錄：`d:\DKTRAINING\ml_streamlit_wine`。
    *   安裝套件：`streamlit`, `pandas`, `scikit-learn`, `xgboost`, `numpy`。
    *   建立 `.venv` (位於 `d:\DKTRAINING`)，並在 VS Code 中設定 Python Interpreter。

### 2. 第一階段實作：核心功能版 (`app.py`)
*   **實作內容**：
    *   載入 Scikit-learn 紅酒資料集。
    *   側邊欄：模型選擇選單、資料集基本資訊。
    *   主區域：資料預覽（Top 5）、各特徵統計值、預測按鈕與結果顯示。
*   **達成目標**：實現基本 ML 模型訓練與預測功能，並在 Port 8501 運行順利。

### 3. 知識內化與學習筆記 (`Learning.md`)
*   **整理重點**：
    *   Q&A 解答：VS Code Interpreter 與 `venv` 的核心觀念。
    *   技術解析：`train_test_split` (模擬考與大考比喻)。
    *   發展策略：Streamlit 在產品週期中的定位（PoC 與 Production 差異）。

### 4. 工具化能力產出 (`convert_to_word.py`)
*   **關鍵進展**：
    *   安裝 `python-docx`。
    *   實作自動轉換腳本，將 Markdown 筆記完美轉為 Word 格式檔案。
    *   成功產出 `Learning_Note.docx` 供課後複習。

### 5. 第二階段迭代：進階美化版 (`app_v2.py`)
*   **升級亮點**：
    *   導入「深酒紅 (#800020)」主題配色與專業表格排版。
    *   加入「互動實驗室」：使用滑桿 (Sliders) 讓使用者自創紅酒進行即時預測。
    *   模型原理說明摺疊選單 (Expanders)。
    *   **並行執行**：計畫配置在 Port 8502 運行。

### 6. 環境除錯與排除 (Troubleshooting)
*   **遇到的挑戰**：
    *   檔案切換時 VS Code 發生 Interpreter 選擇錯誤。
    *   `.venv` 資料夾層級問題（位於上層目錄）。
    *   嘗試使用不同的 Activation 路徑啟動環境。
*   **目前進度**：已修復 `app_v2.py` 遺漏的套件依賴 (`matplotlib`, `seaborn`)，並同步更新 `requirements.txt`。預計安裝完成後即可順利執行。

### 7. 最終驗證與雙版本運行 (Final Verification)
*   **執行狀態**：完成 `app.py` 與 `app_v2.py` 的並行驗證測試，兩版本皆完全克服環境缺乏模組問題。
    *   **基礎版 (`app.py`，Port 8501)**：測試標準流程（選擇模型、分割資料、顯示準確度），運作符合預期。
    *   **專業版 (`app_v2.py`，Port 8503)**：互動實驗系統通過驗證，拖曳滑桿即時帶出對應的分類結果預測，畫面無報錯。
*   **預防措施**：這段開發過程中遭遇的依賴遺漏與環境衝突，已寫入 `Learning.md` 進行深入復盤分析與防範教學。

---
*   **進階功能**：新增 `啟動紅酒App.bat` 腳本，正式實現「滑鼠一鍵啟動」的現代化操作體驗。同步更新 `Learning.md` 紀錄自動化與除錯心法。

---
*記錄者：Antigravity (AI 組手)*
